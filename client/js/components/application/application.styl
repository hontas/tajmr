.application {
  display: flex;
  flex-direction: column;
  height: 100vh;
  margin: 0 auto;
  max-width: $screen-large;
}

.application__init {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

.main {
  flex: 1;
}
